[project]
name = "cpa-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "flask",
  "gunicorn",
  "python-dotenv",
  "requests",
]
